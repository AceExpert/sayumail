import { useState, useEffect, useRef } from "react";

import "./styles/msg.css"

export default function Msg({className, html, style, ref, ...props}) {

    return (
        <div>
            
        </div>
    )
}